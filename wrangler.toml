name = "yabbai-mission-api"
main = "workers/mission.js"
compatibility_date = "2024-01-01"

# Deploy from repo root: npx wrangler deploy
# Secrets (never commit):
#   npx wrangler secret put ANTHROPIC_API_KEY
#   npx wrangler secret put MOONSHOT_API_KEY
# Optional [vars]: ANTHROPIC_MODEL, MOONSHOT_MODEL, MOONSHOT_API_BASE
