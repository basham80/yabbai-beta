{
  "name": "yabbai-beta",
  "private": true,
  "description": "Static site (hub + yabbai/). Workers live under workers/* with their own package.json.",
  "scripts": {
    "build": "node -e \"require('fs').accessSync('index.html'); console.log('Static root OK');\""
  }
}
